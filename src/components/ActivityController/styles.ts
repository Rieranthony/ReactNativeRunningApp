import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  mainContainer: {
    alignItems: 'center',
  },
  buttonsContainer: {
    flexDirection: 'row',
    justifyContent: 'space-evenly',
    marginTop: 24,
    width: '100%',
  },
});
