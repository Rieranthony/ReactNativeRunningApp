import { StyleSheet } from 'react-native';

export default StyleSheet.create({
  base: {
    marginHorizontal: 24,
    padding: 16,
    alignItems: 'center',
    justifyContent: 'center',
    borderRadius: 8
  }
})